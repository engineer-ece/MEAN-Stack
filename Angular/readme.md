# uninstall angular (Ubuntu)

npm uninstall -g @angular/cli

# cache clean

npm cache clean --force

# cache verify

npm cache verify

# install angular

npm install -g @angular/cli@wished.version.here

# create a project

ng new your-app-name

# run angular

cd your-app-name
ng serve --open

# changing the port

ng server --port 3000

